# Core deep learning stack (for YOLOv5/YOLOv8)
torch>=2.0.0
torchvision>=0.15.0

# YOLOv8 (ultralytics) - 학습 및 추론용
ultralytics>=8.0.0

# 이미지/수치 처리
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=9.0.0
scipy>=1.10.0

# 유틸리티
matplotlib>=3.7.0
PyYAML>=6.0.0
tqdm>=4.65.0

# ROS2 Python libraries (for development in venv)
# - 실제 로봇/PC에서는 보통 apt (ros-jazzy-*) 로 설치되지만,
#   가상환경에서 테스트할 때 pip install 시도를 위해 requirements 에도 명시해 둔다.
# rclpy
# tf-transformations